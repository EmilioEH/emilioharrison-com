{
  "name": "@emilio/recipe-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Self-hosted background worker for the two slow Chefboard AI jobs (enhancement, grocery). Subscribes to Firestore via firebase-admin, claims pending jobs, and runs the shared cores from apps/recipes with no waitUntil ceiling. See BACKGROUND-JOBS-VM-PLAN.md.",
  "scripts": {
    "start": "tsx src/index.ts",
    "check:ts": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint src"
  },
  "dependencies": {
    "@google/genai": "1.34.0",
    "cheerio": "^1.1.2",
    "firebase-admin": "^13.6.0"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^4.0.16"
  }
}
